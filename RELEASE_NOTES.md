# 浮岛 v1.0.9 · 模型下载修复（2026-09-24）

- 补齐 Hugging Face 官方分发域名，修复三个模型下载时误报「下载重定向被拒绝」。
- 未知跳转的错误只显示域名，便于定位，不显示带签名的下载参数。HTTPS、固定文件大小与 SHA-256 校验继续生效。
- 升级后重新点击模型安装。已有设置、音频选择和已完成的模型下载保留。Win7 原生版仅同步发行版本。

# 浮岛 v1.0.8 · 本地助手与自定义音频（2026-09-24）

- 标准版新增可选 llama.cpp 本地模型，提供 Qwen3 0.6B / 1.7B / 4B。模型和运行时按需一键下载、校验后启用，不内置，不使用 Ollama。支持取消下载、停止模型并释放内存。
- 识别前台应用名称与已选场景，在灵动岛提供系统音量滑块、媒体播放/暂停、参数化倒计时和日程入口。模型可组合最多三个受支持的快捷操作；按钮显示真实动作和时长，点击才执行，已有倒计时不会被覆盖。
- 轻量规则推荐独立开关，两版默认关闭。Win7 原生版支持规则推荐和音量滑块，仍无需 .NET；不运行大模型。标准版模型适配范围限定 Win10 2004+、x64、AVX2，早期 Win10 / Win7 继续使用轻量快捷与基础功能。
- 倒计时、日程、关机预警分别支持本地 WAV / MP3、试听、音量、恢复默认及停止。关机仅最后 10 秒播放；取消即停。音乐最多播放 30 秒，试听最多 8 秒，失效文件回退默认提示音。
- 不读取屏幕、窗口标题、文档或剪贴板。模型推理仅在本机；下载使用官方固定文件、长度与 SHA-256 校验。模型不能运行生成代码、改写文件或主动关机。

打开控制中心的「本地助手」（教室导航为「助手」）设置。Win7 版从设置进入「场景快捷」「提醒音频」。模型体积、兼容范围与上游许可见 [LOCAL_AI.md](LOCAL_AI.md)，实测结果及限制见 [VERIFICATION.md](VERIFICATION.md)。

# 浮岛 v1.0.7 · 星期循环关机、任务球与 GitHub 自动更新（2026-09-17）


定时关机增加闹钟式重复计划：可选择「每天」「工作日」或单独勾选星期一至星期日，例如只选 1、2、3、4、5。设置一个时、分，点击启用后按所选星期循环执行；修改选项不会直接预约。计划保存在本机，浮岛重新启动后恢复下一次未来时间。关闭计划会停止所有后续重复；仍只在最后 10 秒显示灵动岛预警。电脑未运行、休眠错过或预警不完整时，跳过本次，等待下一个所选日期，不补关机。循环计划需要浮岛在后台运行。

任务缩略球默认放大为电脑 64、教室 88 物理像素，支持 40–160 像素调节。首次升级时将旧版偏小的 30–50 像素设置迁移到新默认值，之后保留个人调整。空闲小点仍使用原有 20% 默认设置。

设置中新增 GitHub 自动更新开关、立即检查和状态反馈。默认启动后约 30 秒检查，之后每 6 小时检查一次；只接受本仓库正式 Release 中版本更高、对应本版的安装器。下载使用 HTTPS，并验证文件大小和 SHA-256；失败保留现有程序，可以手动重试。

安装版下载就绪后，等控制中心及全屏展示关闭、正计时/倒计时（包括暂停任务）结束、单次关机预约取消或完成，再自动安装并静默重启。已保存的循环关机计划允许在距离下次关机超过 5 分钟时更新，更新重启后恢复计划。沿用原安装目录、开机启动及快捷方式选择。自动升级不提权；权限不足会显示失败状态，需手动运行安装器。失败版本不会在后台反复安装重启。便携版可检查和下载安装器，需要手动决定是否安装。

更新需要连接 GitHub；基础计时和提醒仍离线运行。较旧 Win7 若缺少 TLS 1.2 或有效系统证书，检查会失败并显示状态，不降低连接安全要求。v1.0.6 及更早版本没有自动更新功能，需手动安装一次 v1.0.7，此后可自动接收新版。

旧版工程与发行文件已归档至 [main/backup](https://github.com/Watertube-bilibili/free-island/tree/main/backup)，附文件大小与哈希清单。备份验证后清理本地对应旧包，保留当前源码和最新安装包。

# 浮岛 v1.0.6 · 多任务缩略球与大屏触控（2026-09-17）

- 灵动岛收起点新增计时缩略图：中央显示时间，外围细环表示倒计时剩余比例或正计时一分钟内的秒数；沿用现有玻璃材质。打开功能菜单的原悬浮球保持原有用途。
- 一项正计时和一项倒计时可以同时运行，暂停中的任务也保留。两项同时收起时显示数字 `2` 角标，点击同时展开独立任务卡片，各自操作，卡片主体和缩略球继续可拖动。尚不支持任意多项独立倒计时。
- 有计时任务时，缩略球自动使用 30–50 物理像素大小，默认教室 48、电脑 36；任务结束恢复原来的空闲小点。WPF 提供自定义与跟随场景，Win7 在「设置 → 任务球大小」分别保存两个场景的尺寸；教室触控区域仍至少 44 × 44 逻辑像素。
- 教室自定义倒计时改为小时、分钟、秒滑块，加减按钮精确微调。快捷时长先选择，再点击开始；电脑模式保留键盘输入。
- 教室提醒与关机提供触屏日期和时分选择，提醒内容增加常用文字选项。WPF 使用可展开日历及今天/明天快捷操作；Win7 使用独立选择面板，通过前一天/后一天和时分滑块调整后点完成。
- 定时关机只有最后 10 秒才显示灵动岛。此前不占任务缩略球或角标，空闲等待降低检查频率；仍可在控制中心和托盘取消，最后预警卡片也保留取消操作。休眠后错过或未完成预警的计划继续取消。

保留 Win10 1507 / .NET Framework 4.6 兼容路径、Win7 原生免 .NET 版本、玻璃参数和自选安装目录。构建输出：FreeIsland-Setup-1.0.6.exe、FreeIsland-Win7-Setup-1.0.6.exe、两版 Portable-1.0.6.zip、FreeIsland-Uninstall-1.0.6.exe、FreeIsland-Source-1.0.6.zip。

实际验证结果见 [VERIFICATION.md](VERIFICATION.md)。测试在隔离数据和安全模式中进行，不执行真实关机或修改正式自启动；尚未在实体 Windows 10 1507、Windows 7 或学校触摸大屏上验证。

# 浮岛 v1.0.5 · 早期 Win10 水滴与玻璃调节（2026-09-16）

- WPF 应用与安装器最低运行环境降至 .NET Framework 4.6，面向初代 Windows 10 1507（Build 10240）及之后的 17xx / Windows 10 / Windows 11。初代 Windows 10 已自带该运行环境；构建严格使用微软 .NET 4.6 参考程序集。
- Windows 10 Build 10240–19040 新增异步读取下层窗口真实像素的兼容采样，不靠临时隐藏浮窗取样，也不启用屏幕捕获排除。不能提供可用像素的 GPU、视频或透明窗口会保留清透材质。
- Windows 10 2004 / Build 19041 及之后保留桌面采样路线：水滴浮窗可能被录屏或屏幕共享排除；录屏、共享及低配置电脑可使用不采样背景的轻量模式。所有背景像素只在内存中处理，不保存截图、不上传。
- 新增「折射度 / 透明度 / 边缘高光」调节，均为 0–100%，推荐值为 50% / 65% / 55%；实时预览、调整后自动保存、一键恢复推荐。折射度仅在水滴模式生效；关闭玻璃时保留参数。透明度不改变文字衬底的透明度。
- Win7 原生版同步新增「曲面厚度 / 透明度 / 高光强度」，推荐值相同；曲面厚度只调节材质外观，仍不提供真实桌面折射，仍无需 .NET。两版参数覆盖悬浮球、灵动岛和收起的小水滴。
- 保留默认轻量材质、收起点默认 20%（约 6 像素）、大小调节、可拖动靠边吸附、主体文字衬底及安装和计时功能。

WPF 构建首次缺少 4.6 Developer Pack 时，由 `tools/Get-Net46References.ps1` 下载固定版本 1.0.3 的微软官方参考程序集包并校验 SHA-256；这些文件仅供编译，不安装运行环境，不是应用运行依赖。

构建输出：FreeIsland-Setup-1.0.5.exe、FreeIsland-Win7-Setup-1.0.5.exe、两版 Portable-1.0.5.zip、FreeIsland-Uninstall-1.0.5.exe、FreeIsland-Source-1.0.5.zip。实际验证结果见 [VERIFICATION.md](VERIFICATION.md)。尚未在实体 Windows 10 1507、Windows 7 或学校触摸大屏上验证；开发系统上的兼容路线检查不能代替目标系统、显卡与触摸驱动实测。

# 浮岛 v1.0.4 · 水滴收起点（2026-09-16）

- 两版灵动岛收起点沿用「关闭 / 轻量 / 水滴」选项（Win7 第三档为「水滴动效」）：关闭显示黑点，玻璃开启显示透明小水滴。默认轻量，保留已有选择。
- 收起大小仍为 3–20 像素，默认滑杆 20% 约 6 像素；透明交互区域保持电脑 24 × 24、教室 44 × 44 逻辑像素。点击恢复、拖动、靠边吸附及位置记忆保持可用。
- 灵动岛主体标题和详情字号略微增大，增加局部半透明文字衬底，帮助在深浅和复杂背景上阅读；周围保留水滴材质，现代 Windows 水滴模式保留边缘折射。
- Win7 继续提供清透材质与交互动效，不提供真实桌面折射。现代 Windows 水滴模式的系统要求、录屏/共享限制与轻量回退保持不变。

构建输出名称：FreeIsland-Setup-1.0.4.exe、FreeIsland-Win7-Setup-1.0.4.exe、两版 Portable-1.0.4.zip、FreeIsland-Uninstall-1.0.4.exe。验证结果以 [VERIFICATION.md](VERIFICATION.md) 为准；本文不将此前版本的结果计为本轮验证。

# 浮岛 v1.0.3 · 清水水滴

- Win10/11 新增真实背景采样和凸面折射：清透中心、弯曲边缘、轻微放大；按压和拖动只改变材质，文字保持稳定。
- 设置 → 液态玻璃 → 水滴 开启；保留原有用户选择，默认轻量。轻量低占用、无背景采样；关闭为纯色。
- 真实折射需要 Windows 10 2004 或更新系统与桌面合成。水滴浮窗可能不出现在录屏或屏幕共享中；需要共享时选轻量或关闭。背景只在浮窗区域取样并保存在内存，不保存截图、不上传。
- Win7 原生版移除 Aero 模糊及厚重白底，提供清透材质和按压/拖动形变；不提供桌面折射。无 .NET 依赖。
- 保留自选安装目录、升级处理、小黑点默认20%、大小调节和各项计时功能。

本地文件：FreeIsland-Setup-1.0.3.exe、FreeIsland-Win7-Setup-1.0.3.exe、两版 Portable ZIP、FreeIsland-Uninstall-1.0.3.exe、FreeIsland-Source-1.0.3.zip。
# 浮岛 v1.0.2

本版新增自选安装目录，并改进 Win10/11 安装、卸载遇到拒绝访问时的处理。

- 两个版本都支持输入安装路径或点击「浏览」，后续升级沿用已有目录。
- 改到新目录时保留旧程序文件，同一版本的设置和日程继续共用。快捷方式、自启动和卸载记录按实际目录处理。
- Win10/11 普通文件不再执行多余的属性修改；只读文件按需处理，失败时恢复实际改动的属性。区分权限拒绝、文件占用和回滚失败。
- Win10/11 访问被拒绝时，可以选择管理员重试；经 Windows 授权后仍需在窗口确认。应用本身保持普通权限运行。
- 提供独立 Win10/11 卸载工具 FreeIsland-Uninstall-1.0.2.exe，可读取旧版安装记录，默认保留设置与日程。

安装包为 FreeIsland-Setup-1.0.2.exe（Win10/11，需要 .NET Framework 4.8）和 FreeIsland-Win7-Setup-1.0.2.exe（原生 x86，无需 .NET）。同时提供两版 Portable ZIP、源码 ZIP 和校验清单。

保留 v1.0.1 的液态玻璃开关、轻量模式、小黑点默认 20% 滑杆、展开尺寸调节及 Win7 重绘修正。

验证详情见 [VERIFICATION.md](VERIFICATION.md)。本版不修改文件 ACL 或关闭安全软件；具体文件仍可能受到系统访问限制。尚未在实体 Windows 7 教学大屏上验证。
