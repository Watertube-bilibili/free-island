# 本地助手与自定义音频

标准版控制中心的「本地助手」（教室导航中显示「助手」）提供两个独立、默认关闭的选项：轻量场景快捷操作，以及可选本地模型。Win7 原生版在设置中提供「场景快捷」「提醒音频」，只提供规则推荐，不进行模型推理。

## 安装与使用

1. 选择一个模型，点击「一键安装并启用」。运行时约 19 MB，模型另行下载，安装包不内置模型，也不包含 Ollama 代码。
2. 从官方 GitHub / Hugging Face 下载固定版本，校验长度与 SHA-256 后才安装；下载可取消，失败可重试。下载数据放在 `%APPDATA%\FreeIsland\local-ai`。
3. 收起控制中心，切换到播放器、演示或编辑应用并停留片刻。建议会在灵动岛出现，原有计时和关机预警优先。系统音量只在拖动滑块后改变；媒体控制、计时等需点击。
4. 「关闭模型 · 释放内存」停止本次推理服务，并关闭下次启动时自动启用模型的偏好。已下载文件保留，之后可点击「启用已下载模型」。修改模型选择会先停用当前模型。

| 模型 | 量化 | 下载体积（十进制） | 建议整机内存 |
| --- | --- | --- | --- |
| Qwen3 0.6B | Q4_0 | 0.43 GB | 4 GB 或以上 |
| Qwen3 1.7B | Q4_K_M | 1.28 GB | 6 GB 或以上 |
| Qwen3 4B | Q4_K_M | 2.50 GB | 8 GB 或以上 |

建议内存为产品的起步参考，实际可用内存还取决于同时打开的程序。CPU 推理最多使用两个线程、单路请求、1024 token 上下文和 192 token 输出；一次请求上限 30 秒。较慢电脑可只使用轻量规则推荐。

可选模型功能当前限定 **Windows 10 2004 / Build 19041 及以上、64 位系统与 AVX2 处理器**。这是本版保守的适配范围，不是对 llama.cpp 所有构建的最低系统声明。基础计时、玻璃、轻量快捷和音频的系统要求保持原样；学校的早期 Win10 17xx 及 Win7 请使用轻量快捷操作。仅在当前开发电脑完成最小模型实测，未验证所有旧硬件。

## 推荐与可组合工具

AI 只接收前台程序的进程名称和用户选择的教室/电脑场景。不读取窗口标题、截图、文档、剪贴板或浏览历史，也不将应用环境发往云端。下载会连接官方文件托管站；推理只连接随机端口的 `127.0.0.1`，使用每次启动生成的访问密钥。进程退出时关闭所属 llama-server。

模型可为当前场景生成最多三个带名称与参数的快捷操作，组合成为一个临时工具面板。目前允许：音量滑块、播放/暂停、1 分钟至 4 小时的倒计时、倒计时页面和日程页面。界面会显示真实动作与实际时长，模型名称仅作说明。已有倒计时不会被 AI 建议覆盖。此版本不执行模型生成的脚本，不允许模型增加任意系统能力，不自动关机或更改文件。

轻量推荐使用明确的本地规则，并在界面标记「场景快捷操作」或「本地规则」。标准版等待应用稳定约 10 秒，并限制同一应用重复提示；Win7 版有独立的稳定窗口、冷却和失效时间。未知应用可不显示任何建议，浏览器仅靠进程名不能判断正在播放视频。媒体键作用于 Windows 当前媒体会话，可能由其他播放器处理；音量滑块调整系统主音量。

## 自定义音频

为倒计时结束、日程提醒和关机预警分别选择本地 WAV / MP3（单个不超过 100 MB）。保留原文件，文件移动或无法解码时回退默认提示音。设置提供试听、停止、恢复默认和 0–100% 音量；自选音频默认 70%，默认系统提示音跟随系统音量，设为 0 时全部静音。

试听最多 8 秒，普通提醒最多 30 秒，关机只在最后 10 秒内播放。取消或执行关机、确认提醒、退出程序都会停止对应播放。提醒总开关关闭时不自动播放，仍允许主动试听。音频配置分别保存在两版的数据目录 `alert-audio.json`，音频本身不复制、不上传。

## 上游来源和许可

- 推理运行时：[llama.cpp b11146](https://github.com/ggml-org/llama.cpp/releases/tag/b11146)，MIT；使用官方 CPU x64 ZIP，附带的许可文件保留在解压目录。
- [ggml-org/Qwen3-0.6B-GGUF](https://huggingface.co/ggml-org/Qwen3-0.6B-GGUF)、[ggml-org/Qwen3-1.7B-GGUF](https://huggingface.co/ggml-org/Qwen3-1.7B-GGUF)、[Qwen/Qwen3-4B-GGUF](https://huggingface.co/Qwen/Qwen3-4B-GGUF)，模型 Apache-2.0。精确 revision、文件长度与 SHA-256 固定在 `src/LocalAiCatalog.cs`。
- 系统音量使用 Windows [IAudioEndpointVolume](https://learn.microsoft.com/en-us/windows/win32/api/endpointvolume/nf-endpointvolume-iaudioendpointvolume-setmastervolumelevelscalar)。

测试与实测范围见 [VERIFICATION.md](VERIFICATION.md)。模型权重、测试下载缓存与运行时二进制不会进入源码包或安装包。
