# 浮岛 v1.0.4 · 水滴收起点（2026-09-16）

- 两版灵动岛收起点沿用「关闭 / 轻量 / 水滴」选项（Win7 第三档为「水滴动效」）：关闭显示黑点，玻璃开启显示透明小水滴。默认轻量，保留已有选择。
- 收起大小仍为 3–20 像素，默认滑杆 20% 约 6 像素；透明交互区域保持电脑 24 × 24、教室 44 × 44 逻辑像素。点击恢复、拖动、靠边吸附及位置记忆保持可用。
- 灵动岛主体标题和详情字号略微增大，增加局部半透明文字衬底，帮助在深浅和复杂背景上阅读；周围保留水滴材质，现代 Windows 水滴模式保留边缘折射。
- Win7 继续提供清透材质与交互动效，不提供真实桌面折射。现代 Windows 水滴模式的系统要求、录屏/共享限制与轻量回退保持不变。

构建输出名称：FreeIsland-Setup-1.0.4.exe、FreeIsland-Win7-Setup-1.0.4.exe、两版 Portable-1.0.4.zip、FreeIsland-Uninstall-1.0.4.exe。验证结果以 [VERIFICATION.md](VERIFICATION.md) 为准；本文不将此前版本的结果计为本轮验证。

# 浮岛 v1.0.3 · 清水水滴

- Win10/11 新增真实背景采样和凸面折射：清透中心、弯曲边缘、轻微放大；按压和拖动只改变材质，文字保持稳定。
- 设置 → 液态玻璃 → 水滴 开启；保留原有用户选择，默认轻量。轻量低占用、无背景采样；关闭为纯色。
- 真实折射需要 Windows 10 2004 或更新系统与桌面合成。水滴浮窗可能不出现在录屏或屏幕共享中；需要共享时选轻量或关闭。背景只在浮窗区域取样并保存在内存，不保存截图、不上传。
- Win7 原生版移除 Aero 模糊及厚重白底，提供清透材质和按压/拖动形变；不提供桌面折射。无 .NET 依赖。
- 保留自选安装目录、升级处理、小黑点默认20%、大小调节和各项计时功能。

本地文件：FreeIsland-Setup-1.0.3.exe、FreeIsland-Win7-Setup-1.0.3.exe、两版 Portable ZIP、FreeIsland-Uninstall-1.0.3.exe、FreeIsland-Source-1.0.3.zip。
# 浮岛 v1.0.2

本版新增自选安装目录，并改进 Win10/11 安装、卸载遇到拒绝访问时的处理。

- 两个版本都支持输入安装路径或点击「浏览」，后续升级沿用已有目录。
- 改到新目录时保留旧程序文件，同一版本的设置和日程继续共用。快捷方式、自启动和卸载记录按实际目录处理。
- Win10/11 普通文件不再执行多余的属性修改；只读文件按需处理，失败时恢复实际改动的属性。区分权限拒绝、文件占用和回滚失败。
- Win10/11 访问被拒绝时，可以选择管理员重试；经 Windows 授权后仍需在窗口确认。应用本身保持普通权限运行。
- 提供独立 Win10/11 卸载工具 FreeIsland-Uninstall-1.0.2.exe，可读取旧版安装记录，默认保留设置与日程。

安装包为 FreeIsland-Setup-1.0.2.exe（Win10/11，需要 .NET Framework 4.8）和 FreeIsland-Win7-Setup-1.0.2.exe（原生 x86，无需 .NET）。同时提供两版 Portable ZIP、源码 ZIP 和校验清单。

保留 v1.0.1 的液态玻璃开关、轻量模式、小黑点默认 20% 滑杆、展开尺寸调节及 Win7 重绘修正。

验证详情见 [VERIFICATION.md](VERIFICATION.md)。本版不修改文件 ACL 或关闭安全软件；具体文件仍可能受到系统访问限制。尚未在实体 Windows 7 教学大屏上验证。
